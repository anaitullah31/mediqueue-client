export const metadata = {
  title: "Add Tutor | MediQueue - Create a Tutor Profile",
  description:
    "Add your tutor profile on MediQueue to connect with students, share your expertise, manage tutoring sessions, and grow your online teaching presence.",
};

import React from "react";
import AddTutorClient from "../components/AddTutorClient";

const AddTutorPage = () => {
  return <AddTutorClient />;
};

export default AddTutorPage;
